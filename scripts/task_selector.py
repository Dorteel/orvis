#!/usr/bin/env python

# Load the ontology
orka_path = ""

def run_reasoner(ontology, save_path):
    """
    Runs the reasoner on the knowledge graph, and saves the ontology
    on the defined path.

    Returns the materialized ontology.
    """
    pass

def query_knowledgegraph(ontology, query):
    """
    Runs the query on the ontology, and returns the results as a pandas framework
    """
    pass

def update_knowledgegraph(ontology, service, result):
    """
    Takes the ontology, the service that produced the observation and the result, and
    inserts the results into the knowledge graph.
    """
    pass