# This file contains the helper functions for ORVIS
# It takes the models contained in the 'models' folder, and updates ORKA

orka_path = ""
# Load ontology

model_file_path = ""

def load_models(ontology, model_path):
    """
    Takes all the models and loads them into the ontology
    """
    pass